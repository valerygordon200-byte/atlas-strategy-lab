# Relay configuration — copy this file to relay_config.txt and fill in the values.
# relay_config.txt is git-ignored (never commit secrets).
#
# RELAY_URL = where the relay server runs (Tailscale IP preferred):
#   http://100.x.y.z:8787   (Tailscale — same tailnet, any network)
#   http://192.168.1.95:8787 (same LAN)
#   https://xxxx.trycloudflare.com (public tunnel — less secure)
RELAY_URL=http://100.64.0.1:8787

# Shared secret — must match the one used to start the relay.
TOKEN=change-me

# Identity of THIS machine's agent.
ME=desktop-atlas

# Chat dashboard port (this machine).
DASH_PORT=8788

# ---- Autonomous worker (relay/desktop_worker.py) ----
# WORKER_ENABLED=1 runs the always-on executor: it polls the task board
# (coordination/tasks.json) and runs mechanical tasks (created with
# `coord.py new --cmd "python scripts/foo.py ..."`) WITHOUT any Freebuff
# session open. Results are committed to the board and announced on the relay.
WORKER_ENABLED=1

# WORKER_CLAUDE=1 additionally lets the worker dispatch open-ended tasks
# (board entries with --dispatch claude) through the Claude Code CLI
# headlessly. Costs API credits — keep 0 unless you want that.
WORKER_CLAUDE=0
